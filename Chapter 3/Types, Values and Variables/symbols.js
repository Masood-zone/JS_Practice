// Symbols
let strName = "string name";
let symName = Symbol("propname");

console.log(typeof strName);
console.log(typeof symName);

//Need to research more on this, that is the Symbol() function.

// The Global Object
// Constants = undefined, Infinity and NaN
// Functions = isNaN(), parseInt() and eval()
// Constructor function = Date(), RegExp(), String(), Object() and Array()
// Objects = Math and JSON
